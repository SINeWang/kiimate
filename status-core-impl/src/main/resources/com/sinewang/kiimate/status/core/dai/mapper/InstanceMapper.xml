<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sinewang.kiimate.status.core.dai.mapper.InstanceMapper">

    <resultMap id="Instance" type="one.kii.kiimate.status.core.dai.InstanceDai$Instance">
        <result column="id" property="id"/>
        <result column="commit" property="commit"/>
        <result column="owner_id" property="ownerId"/>
        <result column="sub_id" property="subId"/>
        <result column="ext_id" property="extId"/>
        <result column="int_id" property="intId"/>
        <result column="field" property="field"/>
        <result column="value" property="value"/>
        <result column="value_set" property="valueSet"/>
        <result column="value_ref_path" property="valueRefPath"/>
        <result column="value_ref_policy" property="valueRefPolicy"/>
        <result column="operator_id" property="operatorId"/>
        <result column="begin_time" property="beginTime"/>
    </resultMap>

    <select id="selectLatestInstancesByAssetId" resultMap="Instance">
        SELECT mins.id, mins.commit, mins.owner_id, mins.sub_id, mins.ext_id, mins.int_id, mins.field, mins.value ,mins.value_set, mins.value_ref_path, mins.value_ref_policy, mins.operator_id, mins.begin_time
        FROM mm_i_ins mins, km_s_sub ssub, km_s_pub spub
        WHERE ssub.id = #{subId}
        AND ssub.sub_set = spub.pub_set
        AND spub.ins_id = mins.id
        AND ssub.end_time is NULL
    </select>

    <select id="selectLatestInstancesByStatusId" resultMap="Instance">
        SELECT mins.id, mins.commit, mins.owner_id, mins.sub_id, mins.ext_id, mins.int_id, mins.field, mins.value ,mins.value_set, mins.value_ref_path, mins.value_ref_policy, mins.operator_id, mins.begin_time
        FROM mm_i_ins mins, mm_m_sub msub
        WHERE msub.id = #{subId}
        AND mins.sub_id = msub.id
        AND msub.end_time IS NULL
    </select>

    <select id="selectLatestInstanceBySubIdIntId" resultMap="Instance">
        SELECT mins.id, mins.commit, mins.owner_id, mins.sub_id, mins.ext_id, mins.int_id, mins.field, mins.value ,mins.value_set, mins.value_ref_path, mins.value_ref_policy, mins.operator_id, mins.begin_time
        FROM mm_i_ins mins
        WHERE mins.sub_id = #{subId} AND mins.int_id = #{intId}
        AND mins.end_time IS NULL
    </select>

    <update id="updateInstanceEndTimeBySubIdIntId">
        UPDATE mm_i_ins mins
        SET mins.end_time = #{endTime}
        WHERE mins.sub_id = #{subId} AND mins.int_id = #{intId} AND mins.end_time IS NULL
    </update>

    <insert id="insertInstance">
        INSERT INTO mm_i_ins
        (`id`, `commit`, `owner_id`, `sub_id`, `ext_id`, `int_id`, `field`,  `value`, `value_set`, `value_ref_path`, `value_ref_policy`, `operator_id`, `begin_time`)
        VALUES
        (#{id}, #{commit}, #{ownerId}, #{subId}, #{extId}, #{intId}, #{field}, #{value}, #{valueSetHash}, #{valueRefPath}, #{valueRefPolicy}, #{operatorId}, #{beginTime})
    </insert>

    <delete id="deleteInstanceByOwnerId">
        DELETE FROM
        mm_i_ins
        WHERE owner_id = #{ownerId}
    </delete>

</mapper>
